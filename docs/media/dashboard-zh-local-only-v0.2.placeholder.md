# Dashboard Screenshot Placeholder

截图文件 `docs/media/dashboard-zh-local-only-v0.2.png` 暂未提交。

原因：

- 当前阶段以文档先行。
- 真正截图将由用户在本地/真实 VPS 环境手动拍摄后再添加。

当前 Dashboard 已在真实 VPS 完成 local-only 验证：

- 运行模式为本地只读（local-only）。
- 绑定地址示意为 `127.0.0.1:3001`。
- 访问通过 SSH tunnel 展示（不公开公网端口）。

> 此文件只是占位，不代表真实图片。
